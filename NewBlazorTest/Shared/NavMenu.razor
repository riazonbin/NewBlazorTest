<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">NewBlazorTest</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="regPage">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Registration
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="authoPage">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Authorization
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="personalCardPage">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Personal card
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="photoPage">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Photo page
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
